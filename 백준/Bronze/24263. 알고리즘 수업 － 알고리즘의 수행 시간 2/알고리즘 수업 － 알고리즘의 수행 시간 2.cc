#include <iostream>
using namespace std;

void solve() {

	int n;
	cin >> n;

	cout << n << '\n' << "1" << '\n';
}

int main() {
    
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
    
	solve();

    return 0;
}