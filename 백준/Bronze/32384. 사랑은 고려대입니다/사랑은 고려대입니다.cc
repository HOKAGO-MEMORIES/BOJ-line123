#include <iostream>
#include <vector>
using namespace std;

int n;

void solve() {
    for (int i = 0; i < n; i++)
        cout << "LoveisKoreaUniversity" << " ";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    cin >> n;
    solve();
    return 0;
}
